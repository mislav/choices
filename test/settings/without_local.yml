defaults: &defaults
  name: 'Defaults'
  mailer:
    host: 'localhost'
  email: '%{name}@%{mailer.host}'

development:
  name: 'Development'

production:
  <<: *defaults
  name: 'Production'
  mailer:
    host: 'production.com'

test:
  <<: *defaults
  name: 'Test'

index_error:
  name: 'Defaults'
  email: '%{nonexistent.key}'
